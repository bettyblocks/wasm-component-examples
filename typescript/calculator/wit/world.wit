package betty-blocks:calculator;

interface calculator {
  add: func(a: f64, b: f64) -> f64;
  subtract: func(a: f64, b: f64) -> f64;
  multiply: func(a: f64, b: f64) -> f64;
  divide: func(a: f64, b: f64) -> f64;
}

world main {
  export calculator;
}
